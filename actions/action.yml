# action.yml
name: 'Texlive Build'
description: 'Build a document using texlive'
runs:
  using: 'docker'
  image: 'docker://doeringchristian/mszep/pandoc_resume:master'
  entrypoint: '/entrypoint.sh'
